---
const navItems = [
  { title: "Inicio", href: "/" },
  { title: "Proyectos", href: "/projects" }
];
---

<aside class="w-64 h-full bg-gradient-to-br from-[#FF6F61] to-[#E64A45] p-6 shadow-xl">
  <nav>
    <ul class="space-y-4">
      {navItems.map((item) => (
        <li><a href={item.href} class="text-white">{item.title}</a></li>
      ))}
    </ul>
  </nav>
</aside>
