export const Social = (props) => {
    return (
      <a href={props.url} target="_blank" rel="noopener noreferrer" class="text-gray-600 hover:text-[#FF6F61]">
        {props.icon}
      </a>
    );
  };